---
trigger: always_on
---

# Codebase Knowledge Graph Context

When resolving complex bugs, executing architectural refactors, or generating new modules in this workspace, prioritize checking the static dependency mappings generated by Graphify instead of parsing the entire directory blind.

### Reference Mappings:
- Full Dependency Structure: @/graphify-out/graph.json
- Architectural Summary & Reports: @/graphify-out/GRAPH_REPORT.md

### Agent Instructions:
1. Before attempting recursive workspace context lookups, read `@/graphify-out/graph.json` to understand the import/export relationships and high-dependency components.
2. Check `GRAPH_REPORT.md` to identify systemic "God Nodes" and avoid introducing unintended circular dependencies when editing files.
3. If structural codebase changes are made during a task, execute `graphify .` in the terminal to keep the knowledge graph file synchronized.